"""Pytest configuration for async tests.

The ``asyncio_mode = "auto"`` setting in pyproject.toml means all async test
functions are automatically treated as async tests — no ``@pytest.mark.asyncio``
decorator needed. This file is kept as a placeholder for future fixtures.
"""
